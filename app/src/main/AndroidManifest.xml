<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.schoolpickup">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SchoolPickup">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".AdminViewTimetable"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".AddTeacher"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".AddStudent"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".AdminPage"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".EditTimetable"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".TeacherViewTimetable"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".ViewStudents"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".ViewPickupList"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".ParentTracking"
            android:label="@string/title_activity_g_p_s_tracking"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".TeacherEditInfo"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".TeacherPage"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".ViewTimetable"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".ViewTeacher"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".MyStudents"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".PairStudent"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".PickUp"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".EditInfo"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".HomePage"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".Register"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".Login"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>